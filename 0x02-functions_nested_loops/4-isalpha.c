#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
	{

	if (c >= 'a' || c >= 'A' && c <= 'z' || c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
		_putchar('\n');
}
