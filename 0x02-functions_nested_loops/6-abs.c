#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int _abs(int)
	{
	int abs_a = abs(a);
	printf("%d %d", a, abs_a);
	return abs_a;

}
