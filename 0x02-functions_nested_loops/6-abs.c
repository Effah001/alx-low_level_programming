#include "main.h"
/**
 * _abs - returns the absolute value of an integer
 * @a: the int to check
 * Return: the absolute value of int
 */

int _abs(int a)
	{
	if (a >= 0)
	{
		return (a);
	}
	else
	{
		return (-a);
	}
}
