#include "main.h"

/**
 * mul - multipl two integers and returns the result
 * @x: int to be added to y
 * @y: int to be added to x
 * Return: multiplication of x and y
*/

int mul(int x, int y)
	{
		return (x * y);
	}
